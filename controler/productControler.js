const conn = require('./../database/index')

module.exports = {
    addProduct : (req,res) => {

    },
    getAllData : (req,res) => {
        var sql = `select * from manage_product`
        
    }
}